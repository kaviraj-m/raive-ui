{
  "name": "raive-ui",
  "private": true,
  "packageManager": "pnpm@8.6.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "latest"
  },
  "dependencies": {
    "styled-component": "^2.8.0"
  }
}
